# 当前状态

> 给下一次 AI 或人类开发者快速接上上下文用。保持短、准、新。

## 项目一句话

`ai-board` 是一个轻量本地 CLI，用 JSON 作为唯一真相源，为 AI 协作开发提供任务入池、排期、claim、验收、归档和冲突检查。

## 当前能力

- 已创建 Python 包结构：`src/ai_board/`。
- 已实现 CLI 命令：`init`、`add`、`schedule`、`start`、`complete`、`archive`、`block`、`status`、`conflicts`、`render`、`show`。
- 已补充 `goal` 命令，用于从结构化数据设置当前目标。
- 已补充 `skills list`、`skills get core`、`skills get core --full`，用于让 AI 从 CLI 读取版本匹配的使用说明。
- 已新增 `skills/ai-board/SKILL.md` discovery stub，预设 GitHub 安装地址 `https://github.com/dev-null-sec/ai-board.git`，安装策略为 `pipx` 优先、`uv tool` 其次。
- 已实现 `.ai-board/board.json` 读写。
- 已实现 `docs/计划看板.md` 和 `docs/归档计划看板.md` 自动渲染；当前看板各栏目内按优先级 P0、P1、P2、P3 排序显示。
- `ai-board init` 已 CLI 化 `ai-native-project-init` 的核心能力：会生成 `AGENTS.md`、开发规范、当前状态、决策记录、项目方向、页面设计、项目路线等 guardrail 文档；已有文档默认写 `.example`，不覆盖。
- 已实现基于 active task scope 的简单冲突检测。
- 已实现本地 board 写入锁和原子替换，降低多进程同时写 `.ai-board/board.json` 时损坏 JSON 的风险。
- `start` 默认阻止与 active task scope 重叠的任务，支持 `--force` 显式覆盖；`locks` 可查看当前 active scope 锁。
- 任务已支持 `lane`、`source`、`acceptance`、`depends_on` 字段；`add` 可通过 `--lane`、`--source`、重复 `--acceptance`、`--depends-on` 写入。
- 计划看板在每个状态栏目内支持按 lane 分组显示，保持一个 board.json 和一个执行入口。
- 已补充发布 Logo、MIT LICENSE，并重写中英文 README。
- 已创建 `examples/demo-project` 示例项目并跑通过一次完整生命周期。
- 项目自身已接入 `.ai-board/board.json`，并用本 CLI 驱动当前任务、看板渲染和归档。

## 当前技术栈

- Python 3.10+
- 标准库：`argparse`、`json`、`pathlib`、`unittest`
- 打包：`setuptools`
- 运行期无第三方依赖

## 重要目录

- `src/ai_board/`：CLI、数据存储、状态操作、Markdown 渲染。
- `tests/`：单元测试。
- `examples/demo-project/`：CLI 示例输出项目。
- `.ai-board/`：本项目自己的结构化看板数据。
- `docs/`：AI 原生开发工作流文档。

## 最近完成

- 初始化 `ai-board` 项目骨架。
- 按 `AI 原生开发工作流` skill 生成项目规则和 docs。
- 实现最小可用 CLI。
- 用 `ai-board` 自举管理本项目自己的 `T-0001` 任务。
- 跑通测试和示例流程。
- 实现 agent-browser 式分发雏形：skill 只做入口，CLI 提供版本匹配的 AI 使用说明。
- 修正 CLI 安装说明：面向用户级工具安装，不默认鼓励裸 `pip install` 装全局命令。
- 让生成看板按优先级排序显示，避免 P0 任务被创建顺序压到后面。
- 让 `ai-board init` 在新项目中建立完整 AI 原生项目规范文档，完成 `ai-native-project-init` 的 CLI 化主路径。
- 实现 scope lock/lease 雏形：默认阻止多 agent 启动重叠 scope，并增加 `ai-board locks`。
- 支持多泳道看板第一版：任务可记录泳道、来源、验收标准和依赖，看板按泳道分组显示。
- 按发布视角重写 README.md 和 README_en.md，加入 Logo、安装、快速开始、AI agent 使用方式、边界和 License。

## 当前风险 / 未决问题

- CLI 目前是最小版，暂未提供复杂依赖关系检查；锁能力仍是轻量 JSON 级 scope lock，不是 OS 文件锁。
- Markdown 是生成视图，后续需要在 README 和规则中反复强调不要手改生成看板。
- 冲突检测只覆盖路径重叠，不做语义级代码冲突判断。
- GitHub 仓库已发布到 `https://github.com/dev-null-sec/ai-board`，本地 `main` 已跟踪 `origin/main`。

## 下次接手建议

先读 `docs/计划看板.md`，按“正在进行”和“下一批”的顺序推进。GitHub 发布材料已完成并推送，功能开发下一步可推进 `T-0011` 的锁续租/解锁能力；随后再评估 `T-0003` 的 SQLite 后端或事件日志。
